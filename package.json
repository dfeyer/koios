{
  "license": "MIT",
  "dependencies": {
    "elm": "^0.19.0-bugfix2",
    "parcel-bundler": "^1.11.0"
  },
  "scripts": {
    "deploy": "run-s deploy:install deploy:build",
    "deploy:install": "yarn",
    "deploy:build": "run-s build",
    "dev": "parcel serve index.html",
    "watch": "parcel watch index.html",
    "build": "parcel build index.html",
    "format": "elm-format --yes src/"
  },
  "devDependencies": {
    "auth0-js": "^9.10.1",
    "elm-format": "^0.8.1",
    "node-elm-compiler": "^5.0.1",
    "npm-run-all": "^4.1.5",
    "parcel-plugin-elm-hot": "^1.0.1"
  }
}
